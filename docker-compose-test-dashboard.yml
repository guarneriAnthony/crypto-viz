version: '3.8'

services:
  dashboard-test:
    build: ./dashboard-v3
    container_name: crypto_dashboard_test
    ports:
      - "8502:8501"
    environment:
      - MINIO_ENDPOINT=http://192.168.1.76:9002
      - MINIO_ACCESS_KEY=cryptoviz  
      - MINIO_SECRET_KEY=cryptoviz2024
    volumes:
      - ./data:/data
